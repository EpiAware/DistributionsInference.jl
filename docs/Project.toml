# PACKAGE-OWNED — scaffold writes this once and never overwrites it.
# The documentation environment: Documenter + DocumenterVitepress (the org docs
# standard). Add doc-only deps (e.g. plotting, DocumenterCitations) here as the
# docs grow. The package itself is pinned by path via `[sources]`.
# The managed `docs/make.jl` calls `using EpiAwarePackageTools` (the shared docs
# build machinery), so the kit is a docs dep too; until it is registered it is
# pinned by git via `[sources]` (empty for the kit itself, which is the
# package).
# With `ad = true` the env also carries what the kit-managed AD-backends
# tutorial page executes against: the package's `ADFixtures` registry (pinned
# by path, same UUID as the AD test env) plus the benchmark and plotting stack.
# With `benchmarks = true` it also carries `Plots` for the benchmark page's
# combined trend plot (lazily loaded kit-side; absent here, the plot degrades
# to an `@info` note rather than failing the build).
#
# NOTE: the ad-backends tutorial's plotting stack (AlgebraOfGraphics/
# CairoMakie/DataFramesMeta) is removed so this env instantiates:
# `AlgebraOfGraphics` caps `DimensionalData` at 0.28.3 (transitively) while
# this package's hard `FlexiChains` dep needs >=0.29.24 (kit#283;
# ComposedDistributions#147 is the same conflict). The tutorial renders from
# its `TUTORIAL_STUBS` heading meanwhile. Restore the deps and re-add `docs`
# as a workspace member once the upstream ceiling lifts.
[deps]
ADFixtures = "51837e51-2e8d-4c65-a757-038da73554aa"
Bijectors = "76274a88-744f-5084-9051-94815aaf08c4"
ComposedDistributions = "2f642b71-32a9-4f47-97c4-6deb7517f7a7"
DifferentiationInterfaceTest = "a82114a7-5aa3-49a8-9643-716bb13727a3"
Distributions = "31c24e10-a181-5473-b8eb-7969acd0382f"
DynamicPPL = "366bfd00-2699-11ea-058f-f148b4cae6d8"
FlexiChains = "4a37a8b9-6e57-4b92-8664-298d46e639f7"
Markdown = "d6f4376e-aef5-505a-96c1-9c027394607a"
Optim = "429524aa-4258-5aef-a3af-852621145aeb"
Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"
Statistics = "10745b16-79ce-11e8-11f9-7d13ad32a3b2"
Turing = "fce5fe82-541a-59a6-adf8-730c64b5f9a0"
Documenter = "e30172f5-a6a5-5a46-863b-614d45cd2de4"
DocumenterCitations = "daee34ce-89f3-4625-b898-19384cb65244"
DocumenterVitepress = "4710194d-e776-4893-9690-8d956a29c365"
EpiAwarePackageTools = "7aaea248-0d11-4a0d-a7dc-86da30abb951"
Literate = "98b081ad-f1c9-55d3-8b20-4c87d4299306"
DistributionsInference = "205dfa0f-445e-4692-a651-ee32c3bca9ae"

[sources]
DistributionsInference = {path = ".."}
ADFixtures = {path = "../test/ADFixtures"}
# ComposedDistributions is unregistered, so it is pinned by git until it
# registers (mirrors test/Project.toml).
ComposedDistributions = {rev = "main", url = "https://github.com/EpiAware/ComposedDistributions.jl"}
# Until EpiAwarePackageTools is registered, it is pinned by git so
# the env resolves out of the box. Switch to a local path to
# develop the kit alongside this package.
EpiAwarePackageTools = {url = "https://github.com/EpiAware/EpiAwarePackageTools.jl", rev = "main"}

[compat]
Bijectors = "0.13, 0.14, 0.15, 0.16"
ComposedDistributions = "0.1"
DifferentiationInterfaceTest = "0.9, 0.10"
Distributions = "0.25"
DynamicPPL = "0.39, 0.40, 0.41, 0.42"
FlexiChains = "0.4, 0.5, 0.6"
Markdown = "1"
Optim = "1"
Random = "1"
Statistics = "1"
Turing = "0.42, 0.43, 0.44, 0.45"
Documenter = "1"
DocumenterCitations = "1"
DocumenterVitepress = "0.3"
Literate = "2"
