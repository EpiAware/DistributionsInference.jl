# Public API declarations for Julia 1.11+ (public but not exported).
#
# Empty for now: this is a bare bootstrap scaffold
# (`ComposedDistributions#185`). The fit protocol (parameter table, flat
# dimension, reconstruct hook) and the log-density engine land in
# follow-up issues and will be declared here as they are ported.
